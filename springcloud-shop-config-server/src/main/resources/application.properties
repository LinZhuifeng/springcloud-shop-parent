# eureka 把自身注册进 eureka
eureka.client.serviceUrl.defaultZone=http://localhost:5761/eureka/
# server 端口号
server.port=5762
# 应用名
spring.application.name=springcloud-shop-config-server
# profile 采用本地配置文件
spring.profiles.active=native
spring.cloud.config.server.native.search-locations=classpath:/config/
#spring.cloud.config.server.native.default-label=master